# sets

A very simple implementation of a generic set type, based on map\[comparable\]struct{}. It includes basic mathematical operations on sets and set conversions to and from JSON and YAML.

The methods implementing set operations and working with JSON use only the standard library. However, working with YAML requires a third-party library.
